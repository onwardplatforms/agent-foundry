[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    asyncio: mark test as async/await test

addopts =
    --verbose
    --cov=agent_runtime_v2
    --cov-report=term-missing
    --cov-report=html
    --asyncio-mode=auto
